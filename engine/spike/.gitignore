# Compiled binaries
libobs_spike
vkcapture_spike

# obs-ffmpeg-mux symlink (created by `make all` or `make setup_shim`)
obs-ffmpeg-mux

# lib_shim/ and its pulseaudio/ subdirectory are generated by setup_lib_shim.sh.
# Committing the symlinks is fine for reproducibility on this exact host/Flatpak
# version, but they are regeneratable — uncomment the line below to exclude them.
# lib_shim/

# Temporary test helper
test_mux.sh
debug_run.sh
